# Scripts for Openshift Cleanup

Run    `./manage.sh --check ^class-prefix`

To delete resources but not projects run    `./manage.sh --delete-resources ^class-prefix`

To delete projects run    `./manage.sh --delete-projects ^class-prefix`